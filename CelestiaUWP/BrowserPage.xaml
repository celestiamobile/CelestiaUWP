<Page
    x:Class="CelestiaUWP.BrowserPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CelestiaUWP"
    xmlns:celestia="using:CelestiaComponent"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">
    <RelativePanel>
        <muxc:TabView x:Name="TabRoot" IsAddTabButtonVisible="False" SelectionChanged="TabRoot_SelectionChanged" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True">
            <muxc:TabViewItem Header="Solar System" IsClosable="False">
            </muxc:TabViewItem>
            <muxc:TabViewItem Header="Stars" IsClosable="False">
            </muxc:TabViewItem>
            <muxc:TabViewItem Header="DSOs" IsClosable="False">
            </muxc:TabViewItem>
        </muxc:TabView>
        <RelativePanel x:Name="ContentContainer" RelativePanel.Below="TabRoot" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" RelativePanel.AlignBottomWithPanel="True">
            <muxc:TreeView x:Name="Tree" ItemsSource="{x:Bind mRoot, Mode=OneWay}" Margin="16, 16, 16, 0" RelativePanel.Above="ButtonStack" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" RelativePanel.AlignTopWithPanel="True">
                <muxc:TreeView.ItemTemplate>
                    <DataTemplate x:DataType="celestia:CelestiaBrowserItem">
                        <muxc:TreeViewItem ItemsSource="{x:Bind Children}"
                               Content="{x:Bind Name}"/>
                    </DataTemplate>
                </muxc:TreeView.ItemTemplate>
            </muxc:TreeView>
            <StackPanel Orientation="Horizontal" Spacing="8" Margin="16" x:Name="ButtonStack" RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True">
            </StackPanel>
        </RelativePanel>
    </RelativePanel>
</Page>
